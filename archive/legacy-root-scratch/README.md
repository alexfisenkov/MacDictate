# Legacy Root Scratch

This folder keeps old one-off icon experiments and helper scripts that used to live in the macOS project root.

They are not part of the current build, release, signing, notarization, app runtime, backend, or web landing flows. The files are retained only for historical recovery while keeping the root project layout focused on active source-of-truth directories.
